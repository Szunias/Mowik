name: Windows installer

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      version:
        description: "Version without the v prefix, for example 2.4.0"
        required: true
        default: "2.4.0"

permissions:
  contents: write

jobs:
  build-installer:
    runs-on: windows-latest
    timeout-minutes: 90
    env:
      PYTHONUTF8: "1"

    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Resolve release version
        shell: pwsh
        run: |
          if ($env:GITHUB_REF_TYPE -eq 'tag') {
            $version = $env:GITHUB_REF_NAME.TrimStart('v')
          } else {
            $version = '${{ inputs.version }}'
          }
          "RELEASE_VERSION=$version" | Out-File -FilePath $env:GITHUB_ENV -Append

      - name: Verify release version consistency
        shell: pwsh
        run: ./scripts/test-release-version.ps1 -Version $env:RELEASE_VERSION

      - name: Prepare build environment
        shell: pwsh
        run: |
          python -m venv .venv
          choco install innosetup --yes --no-progress

      - name: Build and verify installer
        shell: pwsh
        run: ./scripts/build-release.ps1 -Version $env:RELEASE_VERSION -SkipToolInstall

      - name: Test English and Polish installation and uninstall
        shell: pwsh
        run: ./scripts/test-installer.ps1 -Version $env:RELEASE_VERSION

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: Mowik-${{ env.RELEASE_VERSION }}-Windows
          path: |
            release/Mowik-${{ env.RELEASE_VERSION }}-Setup.exe
            release/SHA256SUMS.txt
          if-no-files-found: error
          compression-level: 0

      - name: Attach files to GitHub Release
        if: github.ref_type == 'tag'
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $tag = $env:GITHUB_REF_NAME
          $installer = "release/Mowik-$env:RELEASE_VERSION-Setup.exe"
          if (gh release view $tag 2>$null) {
            gh release upload $tag $installer release/SHA256SUMS.txt --clobber
          } else {
            gh release create $tag $installer release/SHA256SUMS.txt --title "Mówik $env:RELEASE_VERSION" --generate-notes
          }
